import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["lh3.googleusercontent.com", "cdn.shopify.com", "images.unsplash.com", "files.stripe.com"],
  },
};

export default nextConfig;
